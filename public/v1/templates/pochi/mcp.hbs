{
  "mcp": {
    {{#each mcp.servers}}
    "{{@key}}": {
      "command": "{{this.command}}"{{#if this.args}},
      "args": {{json this.args}}{{/if}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
  }
}
