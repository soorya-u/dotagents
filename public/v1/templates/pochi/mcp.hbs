{
  "mcp": {
    {{#each mcp.servers}}
    {{json @key}}: {
      "command": {{json this.command}}{{#if this.args}},
      "args": {{json this.args}}{{/if}}
    }{{#unless @last}},{{/unless}}
    {{/each}}
  }
}
